[package]
name = "dioxus-storybook-build"
version = "0.1.0"
publish = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Build-script story indexer for dioxus-storybook: discovers #[story] functions and emits the registry."
keywords = ["dioxus", "storybook", "build-dependencies", "codegen"]
categories = ["development-tools::build-utils"]
readme = "README.md"

[dependencies]
